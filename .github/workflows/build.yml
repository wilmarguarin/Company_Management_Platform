name: SonarQube Analyze

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build and analyze
    runs-on: [self-hosted, Windows, X64]

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify Python
        shell: powershell
        run: |
          python --version
          where.exe python
          pip --version

      - name: Install dependencies
        shell: powershell
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run SonarQube scan
        shell: powershell
        run: |
          & "C:\sonar-scanner\bin\sonar-scanner.bat" `
            -D"sonar.projectKey=wilmarguarin_Company_Management_Platform_26de2f77-19c7-47e7-ad7e-9f2f8e37d9af" `
            -D"sonar.projectName=Company_Management_Platform" `
            -D"sonar.sources=." `
            -D"sonar.sourceEncoding=UTF-8" `
            -D"sonar.python.version=3.11" `
            -D"sonar.exclusions=**/__pycache__/**,**/*.pyc,**/venv/**,**/.venv/**,**/node_modules/**,**/static/**" `
            -D"sonar.host.url=${{ secrets.SONAR_HOST_URL }}" `
            -D"sonar.token=${{ secrets.SONAR_TOKEN }}"